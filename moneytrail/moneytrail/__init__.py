"""moneytrail — link public money out (awards, payments, payroll) to money in
(political contributions) through entity resolution, then score red flags.

Every flag is a lead, not a finding. See README.md.
"""
